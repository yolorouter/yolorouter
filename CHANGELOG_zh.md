# 变更日志

[English](CHANGELOG.md) · 简体中文

本项目所有重要变更都记录在此。

格式基于 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/spec/v2.0.0.html)。

## [Unreleased]

### 新增

- OpenAI Speech API 形状下的语音生成（TTS）：`POST /v1/audio/speech` 以二进制音频
  应答、边到边转发，header 承诺 hold 到第一个校验通过的字节。多数底座按 OpenAI
  speech 形状对话；SiliconFlow、智谱与 MiniMax 各带自己的方言（MiniMax 走
  `t2a_v2` 端点，到达时解开其 hex 编码信封）。候选按结算供应商自身的计数规则以
  每百万字符计价（MiniMax 在信封自带 `usage_characters` 时按它计），拨号前先做
  key 预算上限预检，语音请求**不**跨供应商故障切换——音色是调用方自己的选择，
  音色不跨厂迁移。语音探针用一次短合成验证音频候选（以及普通底座上的
  speech-only 测试模型）；模型可声明音频输出，批量导入的音频行带单一价格按音频
  计费落地，管理面显示模态徽标、字符价列以及请求日志按计量口径记录的字符数。

- access-info 卡背后的 API 示例。API Keys 页的接入面板现在每个入口协议一行
  base URL——OpenAI（`/v1`）、Anthropic（裸域名）与新增的 Gemini（`/v1beta`）
  行——每行一个 Examples 按钮打开即拷即用示例的弹窗：八个能力组（聊天、图片
  生成与编辑、视频提交/轮询/下载全生命周期、模型列表、Anthropic 原生与 Gemini
  原生聊天、含函数调用的 Responses API）以 curl、Python、Node 以及（仅聊天）
  Go 呈现，带语法高亮与逐块复制。创建 key 对话框的一次性明文步骤会以嵌套对话框
  打开同一弹窗，并把新 key 注入每条示例。

- 0.2.2 以来再添两个原生视频任务方言：Kling（新设计端点，`kling-3.0` 家族——
  0.2.2 已随版发布但漏记在其 notes 里）与 MiniMax（V2 API，
  `MiniMax-H3`/`MiniMax-H3-Max`），视频面在 wan 与 Seedance 之外达到四个方言。

- MiniMax 视频说明。两个模型对档位的映射不同——H3 以 2K 顶档骑在大档位上，
  H3-Max 停在 768P——且 H3-Max 生成 5 至 15 秒的片段，因此对该模型请求 4 秒会被
  以该理由拒绝而不是被静默改写。MiniMax 把拒绝作为真实 HTTP 状态装在 OpenAI
  形状的 body 里；调用方看到状态，厂商措辞留在审计行。MiniMax 上的视频生成只
  扣按量付费余额——订阅、credit 包与海螺资源包都不覆盖 H3 模型。视频探针与
  key 验证的媒体回退在 ark、dashscope、kling 之外覆盖 minimax 底座。

## [0.2.2] - 2026-09-04

### 新增

- DashScope 方言识别覆盖 Model Studio 工作空间域名
  （`{workspaceId}.{region}.maas.aliyuncs.com`），文档同步更新。

- 图片编辑。`POST /v1/images/edits` 接受 OpenAI multipart 上传（参考图、mask、
  prompt）并转发给 OpenAI 兼容供应商、只改写模型字段，重编码结果在故障切换的
  各次尝试间复用。DashScope 供应商经原生多模态生成方言服务，上传图片以 base64
  data URI 携带；该方言没有 mask 字段，因此带 mask 的请求按候选拒绝而不是被
  静默丢弃。结算复用按张规则，审计行渲染的是上传的形状——文件 part 变成大小
  注记——而不是像素。

- 图片流式。images 端点上的 `stream=true` 对 `gpt-image-*` 模型以命名事件 SSE
  透传服务（partial_image 与 completed 事件，生成与编辑两条路由都支持）；其他
  模型维持 400。用量从 completed 事件读取；未完成的交付——上游自己的错误事件、
  始终没完成一张图的流、读取中断——不计费。

- 编辑形状的模型探针带参考图发起（OpenAI 兼容底座用 multipart，原生方言用
  data-URI 内容项），因此导入如 qwen-image-edit 时不再把编辑家族自身的输入
  规则当作探针失败。

- 视频生成。`POST /v1/videos` 提交一个 OpenAI Videos 任务并立即返回可轮询的
  任务资源（`GET /v1/videos/{id}`，完成的片段在 `GET /v1/videos/{id}/content`）；
  两种 body 形状——官方 SDK 的 multipart 与纯 JSON——解析为同一请求，任务状态
  停留在 SDK 严格的四值词汇表内，内部已退役状态以 `failed` 加错误码传递。两个
  任务方言原生服务：DashScope（wan 家族、拆分请求形状）与火山引擎 Ark
  （Seedance、统一端点），含参考图；其他底座按候选拒绝，已提交的任务不会被
  拿去别处重提。

- 视频结算。候选在分辨率分档表里声明按秒价格；提交那一刻定价（快照）并连同
  所有未完成任务的占用上限一起预留在 key 预算内，超预算的提交以完整算术回应
  429。费用只落一次——首次观察到完成时，无人轮询则由 reaper 对账——按上游
  实际上报的秒数计；失败、取消与过期的任务不计费。

- 视频探测。视频专用模型由被路由请求实际跑的对话验证：一次上游接受的提交加
  一次它应答的任务查询（含 `RUNNING`）——超出探针预算时长的渲染是健康映射，
  不是坏映射。

- 分析里的视频成本。每个视频任务保留其提交的请求 id，结算落账时回填该请求
  日志行的成本——几分钟或几天后，含 reaper 对账的任务——因此仪表盘与成本统计
  从每请求账单早已写入的同一批行里读视频费用。列出现之前创建的任务结算与
  从前完全一致，不做投影。

- 管理控制台的视频定价。模型列表中视频模型带视频徽标，输出模态选择器提供
  视频选项（并按服务端词汇表保持互斥），视频计费候选按每秒渲染计费、每个
  分辨率档一行，候选编辑器新增按秒价格表（分辨率、成本、售价）及与服务端
  相同的本地守卫。

### 变更

- 价格目录刷新管线搬去独立数据仓
  （[yolorouter/price-catalog](https://github.com/yolorouter/price-catalog)）；
  本仓不再接收每日价格刷新提交，嵌入种子改为每个版本刷新一次而非每日。

### 修复

- 视频预算闸门现在在任何拨号发生之前运行。它原来住在任务创建处——在上游提交
  之后——于是超预算的提交被 429 拒绝、供应商却照样渲染了任务，费用落在运营者
  头上，成为一个没人能轮询或下载的孤儿。现在门禁用模型已启用视频候选中最便宜
  的估算预检（同一套算术、同一错误形状），只有所有候选都破顶才拒绝；任务创建
  处的精确闸门仍是被路由候选自身价格的裁决者。预检确凿的拒绝不再进入尝试环
  路——那条路径的副作用曾把预算拒绝记成与 200 并存的矛盾 `conn_error` 结局
  （精确闸门在那里仍可拒绝，设计如此，在预检刻意沉默的窗口里）。

- 未知管理端路径不再渲染空白页：重定向到首页，由鉴权守卫接管。

- 价格目录里的缓存价现在能反序列化了：JSON 键名是 camelCase 而 Go 读取方要
  snake_case，因此嵌入种子与 live 刷新目录两路的 `cache_write`/`cache_read`
  预填建议一直是空的。

## [0.2.1] - 2026-09-01

### 新增

- 图片生成成为一等路由目标。供应商把图片模型挂在 `/v1/images/generations` 之后，
  以 OpenAI images 形状或 DashScope 原生多模态生成方言对话、按供应商选择；凭据
  探针与交付说同一方言，因此一把 key 探测通过就意味着一张可交付的图。图片模型
  按质量/尺寸分档表以每张计价而不是按 token（迁移 00039-00040）；每条请求日志
  携带为它定价的结算快照——维度、交付张数、单价——分档改动之后每张成本仍可审计。
  输出模态声明流经模型新建、编辑、批量创建与批量导入，决定一个模型的池可用哪些
  端点，批量导入拒绝与模型已声明模态相矛盾的行，并在导入进度视图里展示跳过及
  原因。供应商详情页对图片计费映射按张计价并按计费模式给未定价行打标。

## [0.2.0] - 2026-08-28

### 新增

- 供应商及其 key 现在可以删除。删除供应商在一个事务里移除其 key 与模型候选，
  请求历史全量保留——报表继续以已删供应商 id 计流量、名字为空，日志详情仍显示
  记录在案的供应商/key 名。确认对话框预览波及面（key、候选、受影响模型、将被
  打成无可路由来源的模型）并要求重输供应商名。key 随时可删，摘掉供应商最后一把
  可用 key 时给出警告。迁移 00036 解除了请求历史对供应商的外键；SQLite 上这会
  重建 request_logs 表，瞬时需要与该表体量同量的空闲磁盘。

## [0.1.9] - 2026-08-26

### 新增

- 请求日志记录每个请求结算所用的确切价格（输入/输出/缓存读/缓存写，迁移 00033），
  日志详情页展示该快照——目录价格变动后，历史成本仍可审计。

- 仪表盘为所选时间窗新增仅管理员可见的已验证缓存经济学行：命中率、净节省、读
  节省与写溢价，只对流量与定价真正支持缓存的供应商计算，并披露因缺缓存计量或
  价格而被排除的供应商（新 `/api/analytics/cache-stats`；迁移 00034 的部分索引，
  PostgreSQL 上并发创建）。

- 缓存命中率与净缓存节省作为列进入每个分析报表维度与成本下钻表；CSV 导出在既有
  列后追加两个已结算金额。共享的净节省卡加入分析总览行与成本页总览行，所有缓存
  数字在窗口尚未真正记录到缓存计量之前一律渲染破折号——绝不伪造 0% 或 ¥0.00。

- 终端输出压缩覆盖更多 runner：pytest、vitest / jest 与 npm / pnpm install 日志
  把通过的检查与噪声行折叠成计数标记，失败、错误与摘要原样保留，go test /
  pytest 的折叠标记现在携带被折叠内容的最慢耗时。

- 初始安装管理员可以在用户页编辑其他账号的显示名与邮箱（两者原本是一次性写入：
  创建时设定或从身份 provider 首次登录快照）——仅目录信息，目标账号的会话不受
  影响。

### 变更

- 成本优化页的 Concise Output 卡片现在报告所选时间窗的周期累计节省成本与输出
  token（此前是每百万 token 的投影）。
- 新建模型对话框每次打开时调度模式默认 Balanced（按调用方 key 分摊）。Failover
  仍可选，省略该字段的请求其 API 默认仍是 `failover`。
- 新建模型对话框的智谱 GLM 预设组把 `glm-5.3` 与 `glm-5-turbo` 排在前面；此前
  列出的模型全部保留可选。

### 修复

- Gemini 与 Responses 协议的供应商 key 测试现在只对真实成功 body 认证 key：上游
  以 200 回占位或空响应不再把 key 标记为已验证。此变更前记录的结果可能提示重新
  测试。

## [0.1.8] - 2026-08-25

### 新增

- 管理员可以直接在用户页创建本地用户名/密码账号——新「添加用户」对话框创建一个
  用普通登录表单登录的启用成员，可选显示名与手输或随机生成的初始密码（管理员
  线下交给用户；只存 bcrypt 哈希）。首次安装账号现在标记为引导账号（新
  `is_bootstrap` 标志，迁移 00032）：它是唯一永远不能被禁用或降级的账号——
  OAuth 故障的逃生门——其余本地账号则与外部供给的账号一样完全可管理（升降、
  禁用）。此前本地密码账号受 schema 约束唯一，管理员无法创建。创建对话框还接受
  可选的信息性邮箱（仅记入目录；永不发信），引导管理员可以在同一页重置其他本地
  账号的密码——被提升的管理员管理角色与状态但从不管理凭据，重置会终结目标的
  全部在线会话。

- API Keys 页现在展示客户端该指向哪里：网关 base URL 的 OpenAI 兼容（`{base}/v1`）
  与 Anthropic 兼容两种形态，外加一条即拷示例请求。地址在服务端解析——配置了
  `server.external_url` 就用它，否则从请求推导——因此在反代、内网与容器部署上
  也正确，那里控制台自身的 origin 并不是网关的。创建 key 对话框在一次性明文
  步骤重复展示该地址、示例里已填好新 key，CC-Switch 配置导出现在携带同一解析
  地址而非控制台自身 origin。此前这些 key 对应的地址在控制台无处可见，导出的
  配置可能指向一个网关并不应答的地方。

- 模型映射可以从供应商批量导入。供应商页列出上游提供的能力并在一个事务里导入
  所选，整批价格一次解析（先已录入价格、后内置目录）。导入的映射以禁用且未验证
  状态到达，探针通过之前不会有任何路由。模型名现在允许斜杠命名空间
  （`vendor/model`），`GET /v1/models/{model}` 能解析这类 id。

- 导入的映射由后台探针队列验证，导入时武装了自动启用的映射在通过后自动启用
  （迁移 00027-00030）。队列靠从武装行重新推导自身来挺过重启，显式禁用永远压过
  仍在途的探针——包括别的实例或旧二进制启动的。

- 模型可以选择它在供应商之间如何挑选（`scheduling_mode`，迁移 00031）。默认
  `failover` 与从前行为完全一致。`balanced` 把调用方 API key 分摊到一个模型的
  各供应商上、让每个 key 留在落到的供应商那里，于是 key 的流量在上游保温而不
  来回跳；被证明走不通的供应商被短暂隔离，key 重新绑到真正服务过它的那家。

- 成本优化页现在估算 Concise Output 省多少，以每百万输出 token 金额表示：窗口
  已定价流量的加权输出价乘一个实测系数。节省以两张卡展示、每个开关一张——输入
  压缩的实测汇总，以及这个投影连同系数、已定价量与定价覆盖率。系数是五个模型
  配对开/关基准的中位数，方法与全部 150 组原始测量对完整发表于
  `docs/concise-output-benchmark.md` 及其中文版——数字可以核验，而不是信不信
  由你。

- SQLite 数据库在启动迁移前自动快照，快照写不出来就整体拒绝迁移。快照以所取
  schema 版本命名，每次尝试都刷新，只在迁移完全成功后修剪到最新五个。PostgreSQL
  部署不自动备份——官方镜像不带 dump 工具——因此只在确有升级待做时启动日志才
  提醒跑 `db:backup`。

### 修复

- 更新流量在配置的代理被限流时不再放弃。配置的代理曾是唯一路径；现在直连
  GitHub 作为回退尾随其后。这对安装器默认写入的镜像尤为重要：它以共享出口地址
  应答，其 GitHub 配额被身后的每个部署一起消耗，因此可能被限流成 403，而部署
  自己通往 GitHub 的道路完全健康——控制台却会无限期报「检查失败」，运营者无从
  察觉也无从修复。回退对前面的代理是廉价的，后者保住了四分之三的行走预算。

- 按账号用量报表不再凭空发明账号。未解析到 API key 就被拒绝的请求此前被归进
  一个合成的独立行；那一行不是账号，现在从报表与 CSV 导出中一并去除。上方的
  总览卡仍统计那部分流量，表格上的注释说明了这一点。

- 复制按钮在纯 HTTP 部署上可用了。其中两个直接调用剪贴板 API，浏览器在非安全
  上下文里将其留为 undefined——典型的自托管内网环境——于是那些复制在回退方案
  本可工作时静默失败。

- CC-Switch 配置导出以 id 而非显示前缀命名每个 key，两个不同 key 可能共享前者。

- 打开 Concise Output 不再把两条内置提示示例拼成一句英文
  （`...technical detail.Prefer standard library...`）。

### 变更

- Concise Output 系数重测为 12.6%。第一轮基准只装了开关实际写入的两句话中的
  一句，因此它描述的是一个没有任何部署在用的提示词。对着真实提示词重跑逆转了
  标题结论：现在每个被测模型的中位数都为正，此前一个推理模型测出过负值。已
  发布基准的两个版本都从原始测量对重新生成，并明确标注测的是哪个提示词、哪种
  语言。

## [0.1.7] - 2026-08-19

### 新增

- 更新流量——release 查找、两处资产下载与关于页的版本检查——现在先直连 GitHub、
  失败自动经内置镜像重试，因此 GitHub 慢或被挡的部署不用手改 `update.github_proxy`
  就能升级。显式配置的代理仍是唯一路径，其凭据在错误可能回显的任何地方都被
  脱敏。一个停滞看门狗把绝望的传输变成及时回退而不是烧穿超时：header 或 body
  沉默即杀死死路由，速率投影则放弃一个在剩余时间内确定送不完所报体量的传输
  ——被它杀掉的路由在所有备选都失败后获得一次无投影重试。

### 变更

- 供应商的 key 池现在是容量池：请求轮转起点 key，负载摊到全池而不是堆在第一把
  key 上直到它失败。普通 429 把命中的 key 按上游 `Retry-After` 窗口放上板凳
  （尊重并钳制到 1s–10m；回退 `gateway.key_rate_limit_cooldown`，默认 60s）：
  后续请求优先走更健康的 key，上板凳是降权而非除名——全员冷却的池仍会派发、
  最快恢复者优先，且坐板凳的 key 送出 2xx 即归队。未授权与配额耗尽的 key 保持
  既有持久重测路径；轮转与板凳状态在进程内、重启即清零。
- 时间维度分析报表改为每个报表一条分组查询，而不是每个桶一条——90 天按日窗口
  从最多 90 次数据库往返（30 天按小时窗口 720 次）降到 1 次。

### 修复

- 以 CRLF 行尾（`\r\n\r\n`）分帧的 SSE 流曾被 chat 与 Gemini 流解码器整条静默
  丢弃——它们等 `\n\n`，而那种形状里没有两个连续换行，任何帧都凑不齐。分帧
  现在同等地接受 `\n\n`、`\n\r\n` 与 `\r\n\r\n`。
- 省略冒号后可选空格的 SSE `data:` 帧（阿里云 Anthropic 兼容端点就这么发）现在
  被 Claude、chat 与 Gemini 解码器接受。此前这种流的每一帧都被丢弃，一次完整
  交付、完整计费的补全被结算成 `stream_ended_unannounced`、用量记零。
- 关闭状态的供应商上的候选不再进入路由链。它原来在行走时被走过再跳过，花掉
  请求预算里的一次探测、给每个排在活供应商前面的禁用供应商写一行 "provider
  disabled" 尝试——数量一多，请求还没走到本可服务它的供应商预算就花光了。当
  没有可路由候选剩下时，调用方现在得到「配置关闭」应答（503 "no enabled
  route"）而不是那个报告无人经历过的上游故障的 502。

## [0.1.6] - 2026-08-16

### 新增

- 多用户账号。单管理员控制台长成双角色团队系统：管理员保有完整控制台，成员获得
  自助视图（总览、用量、成本与自己的 API key）且由服务端强制——成员发出的每个
  查询都钉在自己的账号上，供应商/上游细节永不出现在成员响应里，够别的账号的
  key 与不存在的 key 无法区分。
- 任意 OAuth2/OIDC provider 的外部登录（authorization-code + PKCE）。供应商在
  控制台配置（一键 OIDC discovery）；首次登录自动建号。原密码账号保留为本地
  逃生门。
- 管理员的用户目录：每账号的来源（本地/登录 provider）、角色、状态、key 数、
  累计消费与最后登录，附启用/禁用与升降操作。禁用账号会删除其会话并立即、
  可逆地关停其全部 API key；刻意不提供删除，因此 key 与请求历史永远可归属。
- 账号维度贯穿控制台：API key 与请求日志携带所属账号，所有仪表盘/分析/成本
  视图可按账号过滤。

## [0.1.5] - 2026-08-14

### 新增

- 官方 Docker 部署。每个版本现在同时向 `ghcr.io/yolorouter/yolorouter` 发布
  多架构（amd64/arm64）镜像（`latest` 加精确版本 tag），仓库新增独立多阶段
  `Dockerfile`——普通 checkout 直接 `docker build .` 即可——与单文件
  `docker-compose.yml`：单服务、一个绑定挂载装下全部状态（生成的配置与 SQLite
  数据库）、健康检查、失败自动重启。容器内版本检查继续工作，原地自更新则让位
  于拉取新镜像。

- 控制台现在可以原地更新 Yolorouter。侧栏新增「关于」入口（有新版本时带新版本
  指示），其页面显示版本、按需检查更新并提供一键更新：服务端下载并校验 release、
  换二进制、重启自身，由服务管理器把新版本拉起。原地换二进制不合适的运行时——
  容器、Windows、带 Linux file capabilities 的二进制、非 release 构建——得到
  相应的升级提示而非按钮，容器里的 `yolorouter update` 现在指向拉取新镜像。

- 看不了图的模型现在也能回答带图请求。在控制台声明模型不能读图并选一个视觉
  兜底模型：网关用该模型描述每张传入图片（按其自身记录的请求计费到调用 key、
  关联到父请求）并把描述作为文本转发——回答仍来自调用方点名的模型。未配置
  兜底模型时，图片变成解释性占位而不是让请求在上游失败。未声明的模型绝不动。
  请求日志给 describe 子调用打标、链接到父请求、可按来源过滤。

## [0.1.4] - 2026-08-12

### 新增

- 控制台的危险操作现在先说清它碰到什么再动手。禁用模型会列出白名单点名它的
  API key、多少全放行 key 也能调它、上周有多少请求点过它；重命名模型以实时
  流量数领衔——白名单跟着模型走、调用方却按名索骥；禁用供应商点名在其上有
  候选的模型、标出哪些会因此彻底失去可路由来源、并点名白名单含这类模型的
  key。模型详情页的影响页签展示同样事实，不再是占位符。
- 无法被路由到的候选现在按运营者需要修理的顺序说出堵在哪——供应商关了、没有
  可用 key、缺供应商侧模型名、缺探针、或它自己的开关。
- 模型列表可按供应商过滤，一眼回答「哪些模型经它路由」。
- 列表行现在原地展开。模型行按路由顺序展开完整候选链——供应商、供应商侧名称、
  网关是否真能路由过去，不能时给出堵因。供应商行展开镜像视图：经它路由的每个
  模型及各自映射的供应商侧名称。
- 为候选挑供应商现在先看清要接手的是什么：下拉里的每个选项携带供应商的实时
  状态（或关闭时的 "disabled"）与真正可用于路由的 key 数，于是哪儿都到不了的
  候选在保存前就可见。
- 仪表盘与分析报表可下钻。仪表盘上每张 KPI 卡、趋势图的每一天、上游状态数字
  与 top 调用方行现在都打开解释它的页面——目的地是请求日志时携带仪表盘所选
  时间窗；成本与 token 卡按目标页面自己的默认窗口打开。四张报表表的每一行都
  打开预过滤到该行模型、供应商、key 或时间桶的请求日志（叠加报表自身过滤）。
  仪表盘区块可键盘操作；表行遵循控制台既有点击约定，趋势图为 canvas（仅指针）。
- 请求日志里合并的「尝试」计数拆成它原本掩盖的东西：换 key（同供应商内轮转
  key）与故障切换（换供应商）是不同事件、不同修法。它们以供应商列上的徽标
  浮现——该列现在同时点名实际服务请求的供应商侧模型——并完整呈现在新的逐行
  展开里，连同完整请求 id 与缓存 token。表格本身重做到永不横向滚动。列表还
  可按 API key 前缀与入口端点过滤（固定路径精确匹配，Gemini 兼容家族按其
  /v1beta/ 前缀，因为这些路径内嵌模型名），CSV 导出以拆分列取代合并计数。
- 请求日志新增成本过滤器（已定价/成本未知）——仪表盘的成本未知数字下钻到的
  正是它所统计的行。
- 供应商报表展示每个供应商造成多少次故障切换——记在让请求失败的那家头上，
  而不是救场的那家，同供应商内的 key 轮转不计。一个被所有请求躲开的供应商
  现在也会出现在报表里、尽管它什么都没服务，因为那正是值得注意的那家。
- 调用方排行现在以消费而非调用量领衔——最该先看的是花得最多的那个——调数列
  可排序以回到旧序。模型与供应商报表表获得同样可排序的调数/成本表头（默认序
  不变），移动端卡片没有表头，一个排序选择器覆盖全部三个页签。时间页签保持
  时间序。
- 供应商 key 的行现在显示其最近一次测试的结果类别——限流、不可达、配额不可用
  等——并附该类别的可操作提示。此前类别在 toast 里一闪而过，刷新页面就只剩
  通过/失败。
- 没有可用路由的模型现在告诉调用方是哪种不可用："no enabled route"（运营者
  关了路由）还是 "routes not verified yet"（探针还没通过过）。此前两者共用
  一句 "model is not available"。
- 以配额耗尽 body 应答 429 的上游 key（OpenAI 的 insufficient_quota，或
  quota/billing/credit 信息）现在被标记重测并移出路由，与 401 完全同款——此前
  它一直被供给，每个请求都在它身上烧一次尝试。普通限流 429 仍只轮转不标记，
  因为它会自愈。
- 每 key 的 TPM 限额现在真正生效。已结算用量累计进每 key 的分钟窗口，窗口到达
  配置上限后请求以 429 拒绝——不做 prompt token 预估，因此并发突发可能先超窗
  一次才开始拒绝。此前控制台接受并存储限额但从未有东西执行它。

### 修复

- 请求的输出量超过服务候选的上限时，现在被压到该候选的天花板而不是照原样转发。
  上游对超大的上限各执一词——钳制、拒绝、或收下并计费——于是同一请求落到不同
  供应商头上费用可能天差地别。
- 以 `max_completion_tokens` 声明输出上限的 OpenAI 形状请求，在前往说另一种
  形状的供应商途中不再丢失它。此前只读旧的 `max_tokens` 拼法，于是恰恰是最新
  请求——推理模型要求此字段并拒绝旧拼法——的上限在协议转换中消失、由供应商
  自己的默认值接管。两者都写时取较低者。

### 变更

- 中转内核继续生长扩展点而非功能代码。能力现在可以在任何候选被选出之前改写
  调用方 body（输入压缩即由此工作）、修复被上游拒绝的 body 并对同一候选重试、
  以及在价格已知后被第二次征询准入——预留终于可以计算的那个时点。供应商携带
  一份健康记录供决策表记账失败，每个请求携带由该表定价的重试预算。

## [0.1.3] - 2026-08-09

一个叠在 0.1.2 上的功能与加固版本：控制台适配移动端、API key 创建后可重看、
供应商价格自动刷新，中转内核围绕显式扩展点重构，并修复故障切换、用量记账与
CLI 的部署处理。

### 新增

- 控制台全面的响应式移动布局：页面、表格与对话框在桌面断点以下自适应，动作统一走底部弹层模式。
- API key 创建后可以在列表页完整重看与复制，展开的 key 不进缓存。导入 key 到 Claude Code Switch 会预填模型并优先选真正可用的那个。
- 供应商价格目录经 worker 每日自动刷新，供应商表单里的价格预填提示跟随刷新后的目录。
- 请求日志：流式 body 查看器逐帧渲染捕获的 SSE 流 body；body 查看器新增复制/展开操作。

### 变更

- 中转内核围绕显式扩展点重构——准入、请求改写、响应观察、终态记录——配一张
  路由判定的封闭决策表，由随套件运行的结构测试守护。不意图改变任何路由行为。
- 跨进其他 handler 领域的服务错误现在映射到自己的错误码，而不是落到通用 500。
- 仪表盘与分析里的「今天」由请求到达时的一次时钟读数计算，日界不再可能在单个
  请求内在本地午夜附近漂移。

### 修复

- 上游以内容检查为由拒绝 payload 时现在触发故障切换，而不是把拒绝抛给调用方、
  任由其他可能接受的供应商被跳过。该拒绝不计入供应商的熔断器。
- 停止原因、用量与缓存 token 计数在每个协议边界上归一，调用方无论被哪种上游
  协议服务都看到同一套账。
- Responses API 接受 content 列表作为 `function_call_output` 输出。
- 失败分发携带的凭据不再随请求日志持久化。
- 已吊销 API key 上的操作被限制在已吊销 key 有意义能做的范围内。
- 供应商探针把超时与不可达目的地分开，测试结果点名真实故障。
- qwen 价格抓取器按表头而非位置对齐列，扛住来源的列序调整。
- `stop` 曾在服务明明运行时报 `no running instance` 且退出码 0——只要它不是从
  服务运行所在目录发起。它在工作目录生成了配置、探测了那个空部署的锁而不是
  真的。它现在什么都不生成：报告它看过的路径，并且——当二进制属于某个安装时
  ——给出该用的 `--config` 行。它还在每个结果旁打印解析到的配置，关于部署的
  回答绝不脱开它所谈论的那个部署出现。
- `db:rollback` 曾在配置不存在时生成配置，这对配置已丢失的部署来说会把它直接
  指回磁盘上仍在的数据库。它现在要求配置存在，并打印它要操作的配置与数据库。
- `db:rollback` 曾对在线服务跑 down 迁移。它现在持有 `serve` 终身持有的同一把
  实例锁——`db:reset` 一直要求的——而不是从运行中的进程脚下删表删列。
- 相对 `sqlite_path` 现在无论配置从哪里到达都解析成一种拼法。锁文件与 Windows
  上 `stop` 通知的关机事件名都从该字符串派生，因此一个部署的两种拼法可能让
  `stop` 通知一个无人监听的事件、对一个健康的服务器超时。

## [0.1.2] - 2026-07-31

叠在 0.1.1 上的维护版本：原生 Windows 安装与开发脚本、托管服务作为供应商预设、
以及用量记账、文档链接与仪表盘趋势图的修复。

### 新增

- Windows PowerShell 脚本：`install.ps1`，可作 `irm ... | iex` 单行命令运行，
  把服务注册为计划任务并按提权与否装为机器级或用户级；以及本地开发用的
  `dev.ps1`。两者都写进了 README 与 CONTRIBUTING。
- 托管服务作为供应商预设。它运行同一个网关，因此四个协议都经一个新的可选
  `extraProtocols` 预设字段对单一 base URL 声明，给 Anthropic、Gemini 与
  Responses 调用方原生直传而非跨协议翻译。不附带 key。
- 预设卡选中后链接到自己的供应商控制台，因为那时只剩 key 一件事要补。
- 供应商列表的操作菜单，直达该供应商的成本详情页。

### 变更

- 自定义系统提示词现在是单个「Concise Output」开关。自由格式的提示词编辑器从
  全局与每 key 的成本优化弹窗中移除；文案来自内置的 concise 与 minimal-code
  预设。
- 新建供应商对话框以托管预设预开，常见路径就是贴 key 保存。选「custom」现在会
  清空预设所属字段（名称、base URL、测试模型、协议配置），而不是只挪高亮——
  此前自定义供应商可能继承预选预设的值。
- README 回归用户视角的概览：项目是什么、怎么跑、完整文档在哪。构建、测试与
  本地开发内容移入 CONTRIBUTING.md。

### 修复

- OpenAI 兼容上游前面接 Anthropic 模型、把净输入计数拷进 `prompt_tokens` 而
  只在 `prompt_tokens_details` 下报缓存部分的，其用量曾被整条丢弃。按 OpenAI
  约定（缓存计数是 prompt 的子集）读取时，这种记录把缓存从一个从未包含它的
  prompt 里减掉、缓存超过 prompt 时变负——随即整条被拒，补全与缓存计数随输入
  一起清零，成功的请求被记成零成本。约定现在在定价或持久化读它之前一次敲定，
  且只在包含式读法被明确排除时才采用。
- 发布的二进制可以自更新了。`update` 与更新检查 API 找 release 的仓库在构建时
  注入，而仓库公开后那个变量从未设过，于是 0.1.0 与 0.1.1 都带着空值发布——
  只有跑 `update` 被告知已禁用的用户看得到。现在发布直接失败，而不是悄悄发布
  一批无法更新的产物。
- README 里的文档链接。文档是主站内嵌的独立应用，因此每个硬编码 `/docs/...`
  链接都经 SPA 回退解析、弹回首页；全部 16 个现在走内嵌路由，保住站点外框与
  语言偏好。
- 仪表盘趋势图：两条 Y 轴各自选刻度数，成本标签落在网格线之间、没有可对照的
  读法。两轴现在共享刻度数并钉住零点。折线平滑也去掉了——按日桶是离散的，
  样条在零到峰值的跳变处过冲，画出没有成本的成本。
- 两个 PowerShell 脚本曾以 GBK 保存。PowerShell 7 无 BOM 时假定 UTF-8，于是
  每条本地化字符串解码成替换字符；5.1 回退系统代码页，只有中文 locale 的机器
  对得上。现在是带 BOM 的 UTF-8，两者都能正确读的唯一形态。

## [0.1.1] - 2026-07-31

Yolorouter 走向多协议：现在接受四种线上协议，并能在去往供应商的途中把任一种
翻译成任一种。新增成本优化特性、更深的成本报表与 Windows 支持。

### 新增

- 协议无关的中间表示，每种协议一组完整编解码器（请求解码/编码、响应解码/编码、
  外加流式解码/编码对）。
- Anthropic Messages 入口：`POST /v1/messages`，以 `X-Api-Key` 或
  `Authorization: Bearer` 鉴权。
- OpenAI Responses 入口：`POST /v1/responses`。
- 原生 Gemini 入口：`POST /v1beta/models/{model}:generateContent` 与
  `:streamGenerateContent`，另接受 `x-goog-api-key` 与 `?key=` 鉴权。
- 每请求协议协商：调用方协议与供应商一致时 body 只改写模型名直通，不一致时经
  IR 往返转换。
- 每供应商协议集合，带每协议端点配置与验证。
- 模型发现：`GET /v1/models` 与 `GET /v1/models/{model}`，按客户端返回 OpenAI
  或 Anthropic 信封。只读——不中转、不花钱。
- 自定义系统提示注入，全局设定、每 API key 可覆盖，以调用方自己的协议形状应用。
- 面向大体积工具输出的输入压缩（`go test` / 构建日志、git diff、grep 结果、
  通用日志），带节省仪表盘与逐请求跳过原因。全局设置、每 API key 可覆盖。
- 成本统计页及每模型、每供应商、每 key 的成本详情页。
- 仪表盘时间范围选择器，范围感知的 KPI 与趋势，独立的输入/输出/缓存 token 卡。
- API key：全模型范围作为显式白名单之外的另一选择。
- 供应商预设目录、实时上游模型列表拉取、带具体失败原因的逐候选能力探测。
- 预设批量建模型、供应商模型名选择器、模型详情页内联编辑。
- 管理页统一的列表过滤器，外加 API key 的归属与状态过滤。
- 可配置的网关超时，覆盖七个独立阶段（连接、TLS 握手、header、首字节、块间
  空闲、单次尝试、整请求），启动时校验顺序。
- `stop` 命令，及 Windows 与 macOS 交叉编译目标。
- 安装与自更新可选 GitHub 镜像（`update.github_proxy`）。
- 启动日志打印监听地址、可点击的 localhost URL 与主内网 IPv4 URL。
- 经 `ccswitch://` 深链一键把模型或 API key 交给 CC Switch 桌面应用。

### 变更

- 以分段空闲保活超时取代单一 120s 上游墙钟，推理模型首 token 前长达数分钟的
  沉默不再被请求中途掐断。
- 候选映射保存时对真实上游验证，取代手工测试按钮。
- 时区改为经 `X-Timezone` 头由浏览器提供 IANA 标识，不再服务端推断。
- 重设计登录与首次安装外壳，共享语言切换；仪表盘空状态改为安装进度漏斗横幅。
- 管理侧栏重组为分组导航；空状态与安装横幅获得语境化图标砖。
- 全部新建/编辑面板统一为弹窗布局，弃用抽屉。
- 重排 API key 表单的自定义提示与过期布局。
- `make build-windows` 现在交叉编译内嵌前端的 amd64 与 arm64 可运行 Windows
  二进制。此前只编译不产物的检查改名 `make build-windows-check`。
- CI 除 Linux 外在 Windows 主机上跑测试套件；只交叉编译抓不住只在运行时分化
  的平台行为。

### 修复

- Windows 构建完全起不来：配置文件的权限检查在 Windows 上拒绝一切文件——Go
  在那里为任何可写文件合成 `0666`、`os.Chmod` 只拨只读位。首跑生成配置又读回
  配置，服务在任何服务发生前就失败了。检查现在按平台拆分；Windows 记一条
  「该平台无法强制权限」的警告并继续。
- 净（非缓存）输入 token 不再从 prompt 总量里扣缓存写。没有协议把缓存写算在
  prompt 内，那笔扣减同时低记了账单的输入行与存储的输入 token 数。净输入现在
  在流式与非流式两条路径上一致持久化。
- DeepSeek 用量映射：`prompt_cache_miss_tokens` 是非缓存 prompt 余额而不是缓存
  写，此前被按缓存写价计价、还把净输入压到零；`prompt_cache_hit_tokens` 在直传
  路径上从未被读，缓存读按全价输入计费。
- 上游 token 计数在计价前先做一致性检查——负数、缓存读大于包含式 prompt、分项
  超过所报总和——现在标记用量未知而不是产出伪造账单。micros 换算带范围守卫，
  越界值无法污染预算记账。
- Claude 流式的 `input_tokens` 在 `message_start` 报零时采纳终态 `message_delta`
  的值——翻译型上游正是这样。总 token 在每个 delta 上按分项重算，而不是停在
  高水位。
- Gemini 的思考与工具 token 计入正确。
- 供应商流式探测现在同时要求非空内容增量与干净终止，发一个增量就挂住的端点不再
  被认证为可流式。探针日志里的供应商 URL 与传输错误串脱敏，嵌在 base URL 里的
  凭据无从泄漏。
- 安装器的错误处理、卸载范围识别与升级安全。
- 前端：TypeScript 类型错误、鉴权布局、图表空状态与导入 toast 行为。

## [0.1.0]

初始版本。核心闭环完成：配置供应商、带故障切换的路由、用量与成本的观测。

### 新增

- OpenAI 兼容网关：`POST /v1/chat/completions`，带流式（SSE）与函数调用
  （`tools` / `tool_choice` / `parallel_tool_calls`）。
- 多供应商路由，有序故障切换，对调用方保持公开模型名稳定。
- 上游 API key 池，限流/鉴权失败/配额耗尽时自动轮转。
- 模型别名（公开模型名 → 各供应商模型 id），带逐候选能力标志（流式、函数调用）。
- API key 管理：模型白名单、请求速率与并发限制、累计预算上限、过期与即时吊销。
  完整 key 仅创建时展示一次。
- 管理控制台：仪表盘、用量与成本分析（按模型/供应商/时间/调用方）、带完整逐次
  尝试路由踪迹的请求日志。CSV 导出。
- 首次安装引导：创建初始管理员、引导式供应商/模型/key 配置。
- 双语管理界面（English / 简体中文）。
- 单二进制、`go:embed` 内嵌 Web 控制台；SQLite 或 PostgreSQL 存储；上游 key
  静态加密（AES-256）。
- 经 `update` 命令与更新检查 API 自更新。

[Unreleased]: https://github.com/yolorouter/yolorouter/compare/v0.2.2...HEAD
[0.2.2]: https://github.com/yolorouter/yolorouter/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/yolorouter/yolorouter/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/yolorouter/yolorouter/compare/v0.1.9...v0.2.0
[0.1.9]: https://github.com/yolorouter/yolorouter/compare/v0.1.8...v0.1.9
[0.1.8]: https://github.com/yolorouter/yolorouter/compare/v0.1.7...v0.1.8
[0.1.7]: https://github.com/yolorouter/yolorouter/compare/v0.1.6...v0.1.7
[0.1.6]: https://github.com/yolorouter/yolorouter/compare/v0.1.5...v0.1.6
[0.1.5]: https://github.com/yolorouter/yolorouter/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/yolorouter/yolorouter/compare/v0.1.3...v0.1.4
[0.1.3]: https://github.com/yolorouter/yolorouter/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/yolorouter/yolorouter/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/yolorouter/yolorouter/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/yolorouter/yolorouter/releases/tag/v0.1.0
